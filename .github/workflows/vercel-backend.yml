name: Deploy Backend to Vercel

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'recruiter-web/**'

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  deploy-backend:
    name: Deploy recruiter-web to Vercel
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: recruiter-web/package-lock.json

      - name: Install dependencies
        run: cd recruiter-web && npm ci --no-audit --no-fund

      - name: Verify types and build
        run: cd recruiter-web && npm run typecheck && npm run build

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Pull Vercel Environment
        run: cd recruiter-web && vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Vercel App
        run: cd recruiter-web && vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel
        run: cd recruiter-web && vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
